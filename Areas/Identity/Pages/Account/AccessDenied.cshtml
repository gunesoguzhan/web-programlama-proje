@page
@model AccessDeniedModel
@{
    ViewData["Title"] = Localizer["Access denied"];
}

<div class="padding-top-120px bg-dark"></div>

<div class="site-section bg-light padding-top-120px" id="contact-section">
    <div class="container">
        <div class="row justify-content-center text-center">
            <div class="col-7 text-center mb-5">
                <h2>@ViewData["Title"]</h2>
                <h4>@Localizer["You do not have access to this resource."]</h4>
            </div>
        </div>
    </div>
</div>