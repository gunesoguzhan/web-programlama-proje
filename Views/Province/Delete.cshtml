@model CarRent.Models.Province

@{
    ViewData["Title"] = "Delete The Province";
}

<div class="padding-top-120px bg-dark"></div>

<div class="site-section bg-light padding-top-120px">
    <div class="container">
        <div class="row justify-content-center text-center">
            <div class="col-7 text-center mb-5">
                <h2>@Localizer["Delete"] @Model.ProvinceName</h2>
                <h3>@Localizer["Are you sure you want to delete this?"]</h3>
            </div>
        </div>
        <div class="d-flex action mb-5">
            <a asp-action="Index" class="btn btn-primary">@Localizer["Go back to list"]</a>
        </div>
        <div class="col-lg-12 col-md-12 mb-4">
            <div class="item-1">
                <div class="item-1-contents">
                    <div class="text-center">
                        <h3>@Model.ProvinceName</h3>
                    </div>
                    <ul class="specs">
                        <li>
                            <span class="spec2">@Html.DisplayNameFor(model => model.ProvinceId)</span>
                            <span class="spec spec2">@Model.ProvinceId</span>
                        </li>
                        <li>
                            <span class="spec2">@Html.DisplayNameFor(model => model.Country.CountryName)</span>
                            <span class="spec spec2">@Model.Country.CountryName</span>
                        </li>
                    </ul>

                </div>
            </div>
        </div>
        <form asp-action="Delete">
            <input type="hidden" asp-for="ProvinceId" />
            <input type="submit" value="@Localizer["Delete"]" class="btn btn-danger" />
        </form>
    </div>
</div>
