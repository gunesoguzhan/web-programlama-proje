@model CarRent.Models.Country

@{
    ViewData["Title"] = Localizer["Delete The Country"];
}

<div class="padding-top-120px bg-dark"></div>

<div class="site-section bg-light padding-top-120px">
    <div class="container">
        <div class="row justify-content-center text-center">
            <div class="col-7 text-center mb-5">
                <h2>@Localizer["Delete"] @Model.CountryName</h2>
                <h3>@Localizer["Are you sure you want to delete this?"]</h3>
            </div>
        </div>
        <div class="d-flex action mb-5">
            <a asp-action="Index" class="btn btn-primary">@Localizer["Go back to list"]</a>
        </div>
        <div class="col-lg-12 col-md-12 mb-4">
            <div class="item-1">
                <div class="item-1-contents">
                    <div class="text-center">
                        <h3>@Model.CountryName</h3>
                    </div>
                    <ul class="specs">
                        <li>
                            <span class="spec2">@Html.DisplayNameFor(model => model.CountryId)</span>
                            <span class="spec spec2">@Model.CountryId</span>
                        </li>
                    </ul>

                </div>
            </div>
        </div>
        <form asp-action="Delete">
            <input type="hidden" asp-for="CountryId" />
            <input type="submit" value="@Localizer["Delete"]" class="btn btn-danger" />
        </form>
    </div>
</div>
